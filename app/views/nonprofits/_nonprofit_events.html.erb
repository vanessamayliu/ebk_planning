<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Events

    <a data-toggle="collapse" href="#nonprofit_events_new_form">
      Add event
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @nonprofit.events.each do |event| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to event, event %>

        <%= link_to event, method: :delete, data: { confirm: "Do you really want to delete this event" } do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="nonprofit_events_new_form">
        <%= render partial: "nonprofits/nonprofit_events_new_form" %>
      </div>
    </div>

  </ul>
</div>
